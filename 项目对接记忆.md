# 项目对接记忆（Project Handoff Memory）

> **给下一个 Claude（或未来的我）**：这个文件是 truth-dare-wheel 项目的状态快照。
> 用户 qiaodaxian233 换对话时，把这个文件丢给你就能直接接着干。
> **第一件事**：读完这份文件再动代码。

---

## 👤 用户信息

- **GitHub**：[qiaodaxian233](https://github.com/qiaodaxian233)
- **仓库**：<https://github.com/qiaodaxian233/truth-dare-wheel>
- **默认分支**：`master`（不是 `main`！）
- **生产部署**：宝塔面板 + PM2，路径 `/www/wwwroot/truth-dare-wheel/`
- **端口**：3101（反向代理到 `http://127.0.0.1:3101`）
- **沟通语言**：中文。**不喜欢冗长解释**，喜欢直接动手 + 简短结论
- **更新方式偏好**：`git pull` + `pm2 restart`，**不喜欢下载 zip 复制粘贴**
- **数据保护意识**：`data/wheel-data.json` 是题库 + 密码，**永远别覆盖**

---

## 🎯 项目是什么

**真心话大冒险炫酷转盘** —— 朋友聚会用的双层大转盘小工具：

1. **玩家页 `/`**：先转主转盘抽「真心话 / 大冒险」，再自动续转对应大转盘抽具体题目。全屏弹窗显示，手机端适配。
2. **控制台 `/admin.html`**：管理题库、调权重、改设置、密码保护。

**技术特点**：
- Node.js 原生 `http` 模块，**零依赖**（package.json 没 dependencies）
- 数据持久化：单文件 JSON（`data/wheel-data.json`）
- 没有数据库、没有 Express、没有任何中间件框架

---

## 📂 项目结构（v2.2.0 — 当前状态）

```
truth-dare-wheel/
├── server.js                  ← 后端,所有 API + 静态服务,放在【根目录】
├── package.json               ← name=truth-dare-wheel-split-pages-optimized,无依赖
├── package-lock.json
├── ecosystem.config.js        ← PM2 配置(name: truth-dare-wheel-optimized, port: 3101)
├── README-部署说明.md
├── data/
│   └── wheel-data.json        ← 运行时数据,【绝对不要覆盖】
└── public/
    ├── index.html             ← 玩家页
    ├── admin.html             ← 控制台
    ├── wheel.js               ← 玩家页 JS(转盘绘制 + 抽奖逻辑 + 随机点名)
    ├── admin.js               ← 控制台 JS(导入/权重/编辑/删除/登录)
    ├── common.js              ← 公共 API 客户端 + sessionStorage token + 工具函数
    ├── style.css              ← 所有样式(包括登录遮罩 / 删除按钮 / 内联编辑)
    ├── login.html             ← ⚠️ 历史遗物,用户自己尝试做密码时建的,现在用不到
    │                              我的方案是登录遮罩浮层在 admin.html 里
    │                              用户说要自己删
    └── assets/
        └── reward-code.jpg
```

**仓库里还有 2 个待清理项**（用户知道，自己删）：
- `public/server.js` —— 跟根目录 server.js 内容一样的冗余文件
- `README-#U90e8#U7f72#U8bf4#U660e.md` —— 文件名乱码版本，跟正常版同内容
- `public/login.html` —— 见上

---

## 🏗️ 架构关键点（不要打破）

### 1. server.js 的双写入路径（极重要）

服务器有**两条写入路径**，对应两种调用者：

| 路径 | 调用者 | 鉴权 | 能改什么 |
|---|---|---|---|
| `POST /api/data` → `writePlayerSafe()` | 玩家页 wheel.js | ❌ 不需要 | **白名单**：仅 `settings.autoSpinNext` 和 `settings.peopleCount` |
| `POST /api/admin/save` → `writeDataInternal()` | 控制台 admin.js | ✅ 需要 X-Admin-Auth | main / truth / dare / settings 全部（除密码外） |

**为什么这样分**：玩家页会调 `apiSaveData()` 来保存抽完后的人数偏好等（见 `wheel.js:563, 590`），如果加上密码保护就会把玩家挡在外面。所以玩家页用一个**字段白名单受限的开放接口**，管理员用一个**鉴权但能改全部的接口**。

**白名单实现**（server.js 中 `writePlayerSafe`）：
```js
const allowedKeys = ['autoSpinNext', 'peopleCount'];
const mergedSettings = { ...existing.settings };
for (const k of allowedKeys) {
  if (k in incomingSettings) mergedSettings[k] = incomingSettings[k];
}
```
玩家就算用 fetch 调 `/api/data` 想 POST `truth: ['HACKED']` 也不会生效。

### 2. 密码绝不通过 GET 暴露

`readDataInternal()` 读完整数据（含 `adminPassword`）；`readDataPublic()` 是它的过滤版，永远剥离 `settings.adminPassword`。**所有 GET `/api/data` 走 readDataPublic，POST 写完后返回也走它**。前端任何时候 fetch `/api/data` 都拿不到密码。

### 3. 密码只能通过专用接口改

- `writeDataInternal()` 不管传进来什么 newData，都从磁盘读现有密码 force preserve（`normalized.settings.adminPassword = existing.settings.adminPassword || ''`）
- 唯一改密码的入口是 `POST /api/admin/password`（需鉴权）→ 调 `setAdminPassword()`
- 这样导入 / 改权重 / 重置都不会意外清空密码

### 4. Token = 密码本身（简化方案）

没用 JWT 也没用 session 表。`/api/admin/login` 校验密码后直接把**密码本身**当 token 返回给前端。前端存 sessionStorage（`tdw_admin_token`），后续请求带 `X-Admin-Auth: <密码>` 头。服务器收到后跟磁盘上的密码字符串比对。

**理由**：项目体量小、内网 / 朋友聚会场景、零依赖。如果以后要上公网，记得：
- 加 HTTPS（反向代理层做）
- 改用 hash + salt 存密码（如 bcrypt）
- token 改成随机字符串 + 过期时间

### 5. Login overlay 在 admin.html 里，不要单独的 login.html

`/admin.html` 加载时 `bootstrap()` → `apiAdminStatus()` 判断是否需要密码：
- 不需要 → 直接渲染
- 需要 + sessionStorage 有 token + token 还有效（用 `/api/admin/login` 复验）→ 直接渲染
- 否则 → 显示 `#loginOverlay` 遮罩（fixed inset:0），输入正确后隐藏

仓库里那个 `public/login.html` 是用户之前自己做的，跟这套机制冲突，**用户自己会删**。

### 6. 题目去重逻辑（容易踩坑）

`normalizeList()` 用 Map 按文本去重，重复保留较大权重：
```js
if (!seen.has(text) || seen.get(text) < weight) seen.set(text, weight);
```

**意味着**：内联编辑时如果改成跟另一行相同的文本，保存后会被合并。所以 admin.js 的 `commitTextEdit()` 在保存前**前端先做重复检测**，重复就回滚 + 提示。

### 7. 内联编辑用 contenteditable + blur/Enter 提交

不是 input 框 —— 而是 `<span contenteditable="true">`。这样不会破坏原有的 `<em>#1</em>` 序号显示。

- Enter → 阻止默认 + 触发 blur → 提交
- Esc → 恢复 dataset.original → blur
- paste → 阻止默认，强制纯文本插入（避免粘贴富文本污染）
- 多行换行折成空格：`newText.replace(/\s+/g, ' ').trim()`

---

## ⚠️ 已知避坑

### A. server.js 必须在【根目录】，不在 public/

代码里 `PUBLIC_DIR = path.join(__dirname, 'public')`，意味着 server.js 必须放在 public/ 的**父目录**。
**用户最初上传的 zip 把 server.js 错放在了 public/ 里**，仓库里现在两个位置都有同名文件（内容一样），但 PM2 启动的是根目录那个。`public/server.js` 是冗余，可删。

### B. `data/wheel-data.json` 永远别覆盖

题库和密码全在这。每次部署 / 推送都要确认这个文件不会被动到。`.gitignore` 应该已经排除（仓库现在没 .gitignore，**TODO**：未来加一个）。

### C. ~~GM 飘屏（broadcast）功能是半成品~~ ✅ v2.3.0 已修复

历史遗留：原版 server.js 没实现 `POST /api/broadcast`，admin 点"发送飘屏"按钮时前端会 `Unexpected token '<'` 报错（因为接口走到 `serveStatic` 返回了 HTML）。

**v2.3.0 修复**：
- server.js 加 `POST /api/broadcast`（鉴权,body `{text}`,空内容 400）
- 写入 `data.broadcast = { id: Date.now(), text, createdAt }`
- `defaultData` / `normalizeData` / `writePlayerSafe` 全都保留 broadcast 字段
- admin.js 的 `apiSendBroadcast` 改用 `adminHeaders()` 带 `X-Admin-Auth`
- 玩家页轮询 GET `/api/data` 时拿到新 `broadcast.id` 就触发飘屏（wheel.js 原有逻辑无需改）

### D. 玩家页 `peopleCount` 防覆盖

`wheel.js:88` 用了 `document.activeElement !== peopleCountInput` 判断 —— 玩家正在输入时不要被 8 秒一次的轮询数据回写覆盖。改逻辑时别破坏这个。

### E. 浏览器缓存

style.css / 各种 .js 文件，服务端给的是 `Cache-Control: public, max-age=3600`（见 `serveStatic`）。改完前端代码用户报问题时，第一句先让他 Ctrl+F5。

---

## 🔌 API 完整清单

| 路径 | 方法 | 鉴权 | 说明 |
|---|---|---|---|
| `/api/data` | GET | ❌ | 读全部数据（自动剥离 adminPassword） |
| `/api/data` | POST | ❌ | 玩家保存（白名单：仅 autoSpinNext / peopleCount） |
| `/api/admin/status` | GET | ❌ | `{ ok, passwordRequired }` |
| `/api/admin/login` | POST | ❌ | body `{password}`，对了返回 `{ ok, token, passwordRequired }`，错返 401 |
| `/api/admin/save` | POST | ✅ | body `{data}`，全量保存（密码字段被 force preserve） |
| `/api/admin/password` | POST | ✅ | body `{newPassword}`，留空字符串=关闭密码保护 |
| `/api/reset` | POST | ✅ | 恢复示例数据（密码保留） |
| `/api/broadcast` | POST | ✅ | body `{text}`(≤200 字)，写入 `data.broadcast`，玩家页轮询触发飘屏 |
| `/api/spin/start` | POST | ❌ | 主转盘启动消耗当前 IP 的 1 次。返 `{ok,remaining,unlimited}`。0次且非无限 → 409 |
| `/api/guess` | POST | ❌ | body `{guess:'left'\|'right'}`，服务端随机生成 `answer`，对当前 IP 应用 delta，返 `{ok,correct,answer,delta,remaining,unlimited}` |
| `/api/admin/ips` | GET | ✅ | 列出所有 IP 记录 `{total,spinCountDefault,spinUnlimited,list:[{ip,count,lastActive}]}` |
| `/api/admin/ip/set` | POST | ✅ | body `{ip,count}` 覆盖某 IP 次数 |
| `/api/admin/ip/add` | POST | ✅ | body `{ip,delta}` 给某 IP 加减次数 |
| `/api/admin/ip/reset` | POST | ✅ | body `{ip}` 删除该 IP 记录 |
| `/api/admin/ip/reset-all` | POST | ✅ | 清空所有 IP 记录 |
| `/api/admin/profiles` | GET | ✅ | 列出所有专属定制版本 |
| `/api/admin/profile/create` | POST | ✅ | body `{name, allowSelfTopup?, selfTopupAmount?, slug?}` |
| `/api/admin/profile/update` | POST | ✅ | body `{slug, name?, allowSelfTopup?, selfTopupAmount?}` |
| `/api/admin/profile/delete` | POST | ✅ | body `{slug}` |
| `/api/admin/profile/reset-claims` | POST | ✅ | body `{slug}` 重置该专属页所有 IP 认领状态 |
| `/api/profile/get` | GET | ❌ | query `?slug=xxx` 单个查询(含 alreadyClaimed 当前 IP 是否已领) |
| `/api/profile/claim` | POST | ❌ | body `{slug}` 玩家访问专属页自动调,领 initialCount(每 IP 仅一次) |
| `/api/profile/topup` | POST | ❌ | body `{slug}` 玩家自助加次数(服务端校验 slug.allowSelfTopup) |

鉴权方式：HTTP 头 `X-Admin-Auth: <密码本身>`。

---

## 🔄 PAT 推送工作流（重要）

每次用户给 PAT 让你推 GitHub 时，**严格按这个流程**（参考 AIM 项目记忆）：

```bash
# 1. 临时目录 clone(PAT 只在 URL 里,不进 git config)
mkdir -p /tmp/push_work && cd /tmp/push_work
PAT="github_pat_xxx"
git clone "https://x-access-token:${PAT}@github.com/qiaodaxian233/truth-dare-wheel.git" 2>&1 | sed 's/[A-Za-z0-9_]\{40,\}/<REDACTED>/g'

# 2. 拷贝改动的文件
cd truth-dare-wheel
cp /home/claude/truth-dare-wheel/server.js              ./server.js
cp /home/claude/truth-dare-wheel/public/admin.html      ./public/admin.html
cp /home/claude/truth-dare-wheel/public/admin.js        ./public/admin.js
cp /home/claude/truth-dare-wheel/public/common.js       ./public/common.js
cp /home/claude/truth-dare-wheel/public/style.css       ./public/style.css
# ⚠️ data/wheel-data.json 永远不要碰

# 3. 配置 git(用户身份)
git config user.name "qiaodaxian233"
git config user.email "qiaodaxian233@users.noreply.github.com"

# 4. commit
git add .
git commit -m "..." 2>&1 | sed 's/[A-Za-z0-9_]\{40,\}/<REDACTED>/g'

# 5. push(注意分支是 master,不是 main!)
git push "https://x-access-token:${PAT}@github.com/qiaodaxian233/truth-dare-wheel.git" master 2>&1 | sed 's/[A-Za-z0-9_]\{40,\}/<REDACTED>/g'

# 6. 清理(PAT 别留在沙箱)
rm -rf /tmp/push_work
```

**所有命令输出都用 `sed 's/[A-Za-z0-9_]\{40,\}/<REDACTED>/g'` 过滤**，确保 PAT 不出现在响应里。

### 撤销验证流程（推送完一定要做）

```bash
# 1. 让用户去 https://github.com/settings/tokens 撤销
# 2. 用一个写操作验证 token 真的死了
mkdir -p /tmp/verify && cd /tmp/verify
PAT="..."
git clone --depth 1 "https://x-access-token:${PAT}@github.com/qiaodaxian233/truth-dare-wheel.git" 2>&1 | sed 's/[A-Za-z0-9_]\{40,\}/<REDACTED>/g'
cd truth-dare-wheel
git config user.email "test@test.com"
git config user.name "test"
git tag _revocation_test_DELETE_ME -m "test"
git push "https://x-access-token:${PAT}@github.com/qiaodaxian233/truth-dare-wheel.git" _revocation_test_DELETE_ME 2>&1 | sed 's/[A-Za-z0-9_]\{40,\}/<REDACTED>/g'
# 期望看到: "Invalid username or token" 或 "fatal: Authentication failed"
rm -rf /tmp/verify
```

---

## 📜 版本历史

### v2.10.0 — 主转盘视觉对半分 + 大转盘每轮抽样 10 道 + 小屏图标紧凑模式（2026-05）

**三个独立改进打包发布**。

#### 1. 主转盘扇区视觉永远 50/50,但概率仍按权重(视觉公平,隐藏偏向)

之前主转盘扇区按权重画(权重 3:1 就画成 3:1 的扇区),玩家一眼看穿哪边概率高。用户要求视觉始终对半分,但概率仍按 admin 设定的权重。

**改动**:`computeArcs(segments)` 加 `useEqualAngles = state.mode === 'main'` 分支。main 模式时每个扇区 = `2π / segments.length`,跟权重无关。`pickWeightedIndex` 没动,所以 selectedIndex 仍按权重抽出,指针 75% 时间落在 dare 那一半圆中心、25% 落在 truth 那一半圆中心。视觉等大,落点频率不等。

真心话/大冒险**大转盘**保持按权重画(权重高的题扇区大,视觉信号有意义)。

`admin.html` 主转盘概率区块的提示文案也同步改了:"玩家页扇区视觉始终对半分(看起来公平),但实际抽中概率按下方权重决定"。

#### 2. 大转盘每轮抽样 10 道(题库可以放几百道)

之前题库有多少题,大转盘就画多少扇区 —— 100 道题就画 100 个细条扇区,玩家根本看不清。现在每次转动前从题库**按权重无放回抽 10 道**作为本轮扇区,题库可以放无限,每轮都新鲜。

**关键设计**:
- `WHEEL_SAMPLE_SIZE = 10` 常量(将来可改成 admin 可配)
- `state.wheelPool = { mode, items }`:当前转盘上的抽样池
- `sampleByWeight(items, k)`:**按权重无放回抽 k 个**,内部用"每次从剩余池里按权重抽一个再删掉"的算法。抽完后按原数组 idx 排序,**视觉上保持稳定顺序**(不会因 random 而乱跳)
- `refreshWheelPool()`:仅 truth/dare 模式抽,main 模式 pool 为空
- `getSegments()`:main 走 `state.data.main`,truth/dare 走 `state.wheelPool.items`
- **抽样调用时机**(3 个):
  - `setMode()` 切换到 truth/dare 时
  - `loadServerData()` 检测到 dataHash 变化 + 当前在 truth/dare 时
  - `spin()` 转动开始前(truth/dare 模式)+ 立即 `drawWheel()` 让玩家看到新扇区再开始转
- 题库 ≤ 10 时 `sampleByWeight` 直接返回全部
- 权重为 0 的题用 0.0001 兜底,避免全 0 时 total 为 0 抽不出

**单测验证**(`/tmp/test_sample.js`):
- 25 道抽 10,3 次结果完全不同 ✓
- 5 道抽 10,返回全部 5 道 ✓
- 1000 次抽 3:权重 10 入选 984 次,权重 0.1 入选 17 次 → 权重生效 ✓
- 抽样后 indices 严格单调递增 → 顺序稳定 ✓

`admin.html` 题库预览区加金色提示告诉主播这个机制。

#### 3. 小屏图标紧凑模式(≤760px 自动启用)

之前手机模式下,topbar 6 个元素(控制台/当前模式/剩余次数/作者主页/赞赏码/邮箱)每个独占一行,占用大半屏。用户要求:小屏改成图标横排,且**支持按钮(👤💛✉️)挪到标题旁边**。

**HTML 结构调整**:
- 把 `.support-top` 从 `.top-actions` 内**挪到 `.brand` 内**,作为 `.brand-title-row` 的 sibling。这样桌面端跟在标题右边,小屏自然紧凑。
- 所有 pill 内的"文字 + emoji"拆成两个 span:`.pill-emoji` + `.pill-text`,方便小屏 CSS 单独控制
- 加 `.brand-text` 容器(flex column),让标题行和副标题各占一行
- 加 `title` 属性,小屏鼠标 hover 也能看到完整文字

**CSS 改动**:
- 新增 `.brand-text` / `.brand-title-row` 默认 flex 样式
- `@media (max-width: 760px)` 紧凑图标块:
  - `.pill-text` 全部 `display: none`(只显 emoji + 关键数字如"主转盘"/"13")
  - `.support-pill` 缩小 padding + min-width,变成正方形图标按钮
  - `.support-top` 改成 inline-flex 横排,背景透明(去掉胶囊容器)
  - `.top-actions` 强制 flex row(覆盖原 980px 断点的 column)
  - `.brand-title-row` 在小屏改成 `justify-content: space-between`,让支持按钮自动靠右
- `@media (max-width: 560px)` 清理掉旧的 grid 布局(`top-actions display:grid; grid-template-columns:1fr`)—— 那条会覆盖紧凑模式;现在保留 mode-tabs/result-modal 等无冲突的样式
- 极小屏(≤560px)pill 再缩一档,logo 缩到 42px

**改动文件**(5 个):
- `public/wheel.js`(computeArcs 加均分分支 + WHEEL_SAMPLE_SIZE + wheelPool + sampleByWeight + refreshWheelPool + getSegments 重写 + 3 处接入)
- `public/index.html`(topbar 结构重组:support-top 挪进 brand;所有 pill 加 .pill-emoji/.pill-text 包裹)
- `public/admin.html`(主转盘概率文案改 + 题库区加抽 10 道说明)
- `public/style.css`(.brand-text/.brand-title-row + 760px 紧凑模式 + 清理 560px 冲突 grid)
- `package.json`(2.1.0 → 2.10.0)
- `项目对接记忆.md`

**注意事项**(下一个 Claude):
- **getSegments 在 truth/dare 模式可能返回原始题库(回退路径)**:当 `state.wheelPool.mode !== state.mode`(刚切换还没 refresh)或 `items.length === 0`(题库为空抽样得空数组)时,回退到 `state.data[mode]`。spin 函数会先 refresh 再 getSegments,所以正常路径走 pool。直接调用 getSegments 的其它路径(如 computeArcs)拿到 pool 即可。
- **selectedIndex 永远基于当前 segments**(可能是 pool 的 10 道,可能是原始的 N 道),所以 `finishSpin(segments[selectedIndex], ...)` 拿到的是抽样池里的那道题,不是原始题库 idx。若将来要把"抽中的是原始第几道题"展示给玩家,需要在 sampleByWeight 返回时同时保存原 idx 给 wheelPool。
- **题库变化要重抽**:已通过 loadServerData 的 changed 标志接入;但如果将来加"主播在 admin 编辑后玩家页主动刷新"的快路径,记得也调一次 refreshWheelPool。
- **主转盘 vs 大转盘的 computeArcs 行为不一致**:main 均分,truth/dare 按权重 —— 这是 by design,看 `useEqualAngles` 注释。改这块务必两个模式都测一遍指针落点。
- **小屏紧凑模式的"剩余"显示**:.pill-text "剩余:" 在小屏被隐藏,只剩 emoji + 数字(如"🎲 13")。如果将来要给数字加单位或更明确的标签,改 .pill-text 显示规则即可。
- **support-top 桌面端位置**:现在桌面端也在标题旁边(不是原来 top-actions 区那个位置)。视觉上更紧凑,但跟 v2.9.0 之前的位置不一样 —— 如果用户反馈"桌面端想要原来的位置",方案是在 980px 以上断点用 CSS 把 support-top 重新定位回 top-actions 区域(用 grid order 或 absolute)。

### v2.9.0 — 专属页 initialCount(挂钩礼物 / 首次自动发放）（2026-05）

**业务背景**:主播在直播间收到玩家送的虚拟礼物(跑车 / 火箭等),按礼物分数换算成抽奖次数(例如 跑车 = 16 次)。需要一种"开链接即得 N 次"的体验:主播创建专属页时填 initialCount=16,把 URL 发给该玩家,玩家一打开就得到 16 次,不用点任何按钮。

**新字段**:
- `profile.initialCount`(0-9999):专属页配置,玩家**首次**用该 URL 访问时自动加到他 IP 的池里
- `profile.claimedIps`(map):记录哪些 IP 已经领取过 initialCount,防止重复领取
  - 同一 IP 在同一专属页上**只能领一次**;主播可点「重置认领」让所有 IP 可以重新领

**与已有 selfTopupAmount 的关系**:两者独立并存
- `initialCount`:**一次性**自动发放,无需点击;访问 URL 就给
- `selfTopupAmount`:**可重复**按钮触发,玩家点一下加 N(N=1-50)
- 主播可以两个都开(初次见面送大礼包 + 后续按礼物补送)

**新增接口**:
- `POST /api/profile/claim` body `{slug}` → 玩家访问专属页时前端自动触发,服务端校验该 IP 是否已在 `claimedIps`,未领则 +initialCount 并标记;已领直接返回 alreadyClaimed=true
- `POST /api/admin/profile/reset-claims` body `{slug}` → admin 重置某专属页的认领状态

**改动接口**:
- `GET /api/profile/get?slug=xxx` 返回新增 `initialCount` 和 `alreadyClaimed` 字段(基于当前请求 IP)
- `POST /api/admin/profile/create` body 接受 `initialCount`
- `POST /api/admin/profile/update` body 接受 `initialCount`

**前端联动**:
- wheel.js `applyProfile()` 成功拿到 profile 后,如果 `alreadyClaimed===false && initialCount>0`,自动 POST `/api/profile/claim`,弹 toast 告知玩家获得了多少次。`profileClaimAttempted` 标志位防止单次会话重复尝试
- admin.html 创建表单 grid 从 4 列改 5 列,加 `#newProfileInitial` input(0-9999)
- admin.js renderProfileList 加「首次给」「已领」两列,「已领」列在 >0 时显示「重置认领」按钮;`updateProfileInitial` / `resetProfileClaims` 两个新函数

**改动文件**(6 个):
- `server.js`:normalizeData profile 加 initialCount + claimedIps;createProfile/updateProfile 支持新字段;新增 claimProfile() resetProfileClaims() 两个 helper;getProfilePublic 加 ip 参数返回 alreadyClaimed;路由加 2 个新 endpoint
- `public/wheel.js`:applyProfile 后自动 claim;profileClaimAttempted 防重;成功 toast
- `public/admin.html`:create form grid 5 列 + #newProfileInitial input;表格表头 7 列
- `public/admin.js`:newProfileInitial DOM ref;createProfile 带 initialCount;renderProfileList 加新列 + 重置认领按钮;新增 updateProfileInitial + resetProfileClaims
- `项目对接记忆.md`

**注意事项**(下一个 Claude):
- **首次给** vs **每次加**:用户语境完全不同。"首次给"是被动收礼,"每次加"是主动按钮。设计上保持独立,不要合并
- `claimedIps` map 不会自动清理(跟 v2.7.0 user 选择"所有 IP 永久保留"一致),靠主播手动「重置认领」
- 改 initialCount **不会**影响已领过的 IP(他们已经在 claimedIps 里了)。这个语义合理:历史礼物不应该被新规则改写。若要让旧 IP 重领,主播点「重置认领」
- 若反向代理没传 X-Forwarded-For,所有玩家被识别为 127.0.0.1,**只有第一个访问的人能领到 initialCount**,后面都被判定 alreadyClaimed。务必配置 nginx

### v2.8.1 — 主界面双列布局 + 创建专属页可定制初始加多少（2026-05）

小版本调整,两件事:

1. **玩家主界面双列布局**:转动次数卡片 + 随机点名卡片**桌面端并排**(>760px),手机端自动堆叠回上下。新增 `.side-by-side-row` grid 容器包裹两个卡片
2. **admin 创建专属页时直接填初始加多少**:之前固定 5,要先创建再到表格里改。现在创建表单加一个 number input(1-50),提交时随 selfTopupAmount 一起传给后端

**改动文件**(3 个):
- `public/index.html`:用 `.side-by-side-row` 包住 spin-control-card 和 picker-card
- `public/admin.html`:create 行 grid 从 3 列改 4 列,加 `#newProfileAmount` input + .profile-amount-inline 容器
- `public/admin.js`:`createProfile()` 读 newProfileAmount 值(默认 5,1-50 范围) + 加 `newProfileAmount` 元素引用
- `public/style.css`:新增 .side-by-side-row + .profile-amount-inline 样式
- `项目对接记忆.md`

### v2.8.0 — 专属定制页(/p/<slug>)+ 玩家自助加次数（2026-05）

**功能**:为特定朋友/客户生成专属 URL。访问后 banner 显示自己的名字(替换"真心话 · 大冒险" 标题),主播可单独开启「自助加次数」让该专属版本的玩家在前台点按钮自助补给。

**数据结构**:
- `data.profiles`(新,顶层):`{ "<slug>": { name, slug, allowSelfTopup, selfTopupAmount, createdAt } }`
- slug 是 URL 段,默认自动生成 6 位 `[a-z0-9]`,主播可在创建时自定义(2-32 位 `[a-zA-Z0-9_-]`)

**关键设计**:
- URL 形式 `/p/<slug>`,server.js 的 `serveStatic` 已经会 fallback 到 `index.html`,无需新增路由
- ⚠️ **`profiles` 永不通过 GET /api/data 暴露**(隐私 + 防探测)。玩家页只能通过 `/api/profile/get?slug=xxx` 查询自己 URL 那个,且返回的字段已经过滤(只 name/slug/allowSelfTopup/selfTopupAmount,无 createdAt 等)
- **spinCount 仍然按 IP 隔离**(v2.7.0 架构不变):profile 只是"皮肤层 + 配置容器",不会让同一个 IP 用不同 profile 累加次数
- 自助加每次 1-50 范围 admin 可配,服务端校验 `profile.allowSelfTopup`,前端按钮只是 UI 提示
- 访问 `/p/<不存在的slug>` 时玩家页显示「⚠ 专属 URL 已失效」但仍可正常游玩(用 IP 默认次数)
- 进入专属页时 wheel.js 把 `<h1 id="pageTitle">` 改成 `"${profile.name} 专属"`,document.title 也跟着改

**新增 admin 接口**(都需 X-Admin-Auth):
- `GET /api/admin/profiles` → 列出所有 profile,按 createdAt 倒序
- `POST /api/admin/profile/create` body `{name, allowSelfTopup?, selfTopupAmount?, slug?}` → slug 不填时自动生成
- `POST /api/admin/profile/update` body `{slug, name?, allowSelfTopup?, selfTopupAmount?}`
- `POST /api/admin/profile/delete` body `{slug}`

**新增玩家接口**(无鉴权):
- `GET /api/profile/get?slug=xxx` → 返回单个 profile 的公开字段或 404
- `POST /api/profile/topup` body `{slug}` → 服务端校验 `profile.allowSelfTopup`,允许则当前 IP `+selfTopupAmount` 次数

**改动文件**(6 个):
- `server.js`:`profiles` 字段 + normalizeData 校验 + 4 个 admin 接口 + 2 个玩家接口 + readDataPublic/writeDataInternal/writePlayerSafe 全部剥离 profiles 防泄露 + getProfilePublic helper
- `public/index.html`:banner 加 `#profileBadge` + `#brandSubtitle`;spin-control-card 加 `#spinSelfTopup` 按钮区
- `public/wheel.js`:`CURRENT_PROFILE_SLUG` 从 `/p/<slug>` URL 解析 + `applyProfile()` 异步拉 `/api/profile/get` 缓存到 `currentProfile` + `applyProfileToUI()` 改 pageTitle/badge/subtitle/topup 按钮 + `submitSelfTopup()`
- `public/admin.html`:新增「🎁 专属定制版本」区块(输入名字 + 一键生成 + 表格)
- `public/admin.js`:`loadProfileList` / `createProfile` / `toggleProfileSelfTopup` / `editProfileName` / `editProfileAmount` / `deleteProfile` / `copyToClipboard`
- `public/style.css`:`.profile-badge`(banner 上的金色徽章) + `.spin-self-topup`(玩家页按钮区) + `.profile-amount` 输入框

**注意事项**(下一个 Claude):
- profile **隐私保护已做好**:玩家只能查 URL 里写的那一个,看不到其它专属页存在
- profile 删除不影响该 IP 已有的 spinByIp 记录(spinCount 持续保留),只是该 URL 后续访问会显示"专属 URL 已失效"
- 自助加是个"绕过主播管控"的口子,主播得想清楚再开;开了后玩家可以无限点(每次 selfTopupAmount,最多 50),**没有节流**。如果以后需要限频,加个 `lastTopupAt` 时间戳判断
- **此功能在 v2.5.0~v2.7.0 期间被 Claude 部分写入沙箱但未提交**;v2.8.0 完成了隐私漏洞修复 + 端到端测试 + 推送上线

### v2.7.0 — Per-IP 转动次数池 + 大后台 IP 管理（2026-05）

**架构性改动**:`spinCount` 从全局共享 → 按客户端 IP 隔离。每个 IP 独立一份次数,互不影响。主播在大后台能看到所有玩过的 IP 并精细管理。

**数据结构变更**:
- `settings.spinCount`(旧,全局) → 废弃
- `settings.spinCountDefault`(新,全局):新 IP 首次抽奖时分配的初始次数
- `data.spinByIp`(新,顶层):`{ "<ip>": { count, lastActive } }`
- **自动迁移**:`normalizeData` 检测到旧的 `settings.spinCount` 字段且没有 `spinCountDefault` 时,把值拷贝过来。已部署用户无感升级。

**关键规则**:
1. **玩家页 GET `/api/data`** 拿到的 `settings.spinCount` 是【自己 IP】的剩余,服务端动态注入
2. **`spinByIp` 永不暴露给前端**(隐私 + 防作弊),只在 admin 接口里返回
3. **惰性创建**:GET 不写盘,**只在 `/api/spin/start` 和 `/api/guess` 时**为 IP 创建记录。所以只有"真正参与抽奖"的 IP 进入 `spinByIp`,纯访客不占用存储
4. IP 识别:`x-forwarded-for` 首段 → `x-real-ip` → `socket.remoteAddress`。宝塔 nginx 反代下能拿到真实 IP
5. IPv4-mapped IPv6 前缀 `::ffff:` 自动剥离
6. 用户选了「所有 IP 永久保留」,**没有自动清理**;主播在大后台用「清空所有 IP」手动处理

**新增 admin 接口**(都需要 X-Admin-Auth):
- `GET /api/admin/ips` → `{ total, spinCountDefault, spinUnlimited, list:[{ip,count,lastActive}] }`,按 lastActive 倒序
- `POST /api/admin/ip/set` body `{ip, count}` → 覆盖某 IP 的次数
- `POST /api/admin/ip/add` body `{ip, delta}` → 给某 IP 加减
- `POST /api/admin/ip/reset` body `{ip}` → 删除该 IP 记录(下次访问重新分配 default)
- `POST /api/admin/ip/reset-all` → 清空 `spinByIp`

**改动文件**(7 个):
- `server.js`(getClientIp + cleanIp + ensureIpEntry + 5 个新接口 + readDataPublic 改签名 + writePlayerSafe 保留 spinByIp)
- `public/index.html`(主界面 spin-control-card **简化为只读** —— 移除输入框/快捷按钮/无限开关)
- `public/wheel.js`(移除 saveSpinControl/scheduleSpinSave 等约 60 行代码;updateSpinCountDisplay 改为纯展示;spinControlHint 根据状态显示不同提示文案)
- `public/admin.html`(spinCount 标签改为「默认初始次数」;新增「🎮 玩家次数管理」区块,带 IP 表)
- `public/admin.js`(新增 ~150 行 IP 管理:loadIpList/renderIpList/ipAdd/ipSet/ipReset/resetAllIps + formatRelativeTime;bootstrap 加载时自动拉 IP 列表)
- `public/style.css`(spin-control-card 简化样式 + 完整 `.ip-table` 表格样式 + 移动端适配)
- `项目对接记忆.md`

**注意事项**(下一个 Claude):
- 玩家页的 `settings.spinCount` 字段**前端代码不必关心是 per-IP 还是全局**,服务端透明处理。改前端时不要把 `spinCount` 当作可写字段(玩家页白名单不含,/api/data POST 改不了)。
- 主播在 admin 大后台改某 IP 的次数 vs 改 `spinCountDefault`是两件事:前者影响特定玩家,后者影响"未来新进来的 IP"。
- 若反向代理没正确设 `X-Forwarded-For`,所有玩家都会被识别为 `127.0.0.1`,共享一份次数 ←【需在 nginx 配置 `proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;`】
- 用户偏好:不喜欢自动清理/过期机制,所有 IP 永久保留直到手动清空。

### v2.6.0 — 主界面转动次数控制 + START 禁用 + 规则调整 + token localStorage（2026-05）

**改动**:

1. **主界面新增「🎲 转动次数」控制卡片**(放在转盘和随机点名之间)
   - 大数字显示当前剩余(无限模式显示 ∞)
   - 数字输入框,输入后 600ms debounce 自动保存
   - 「+10/+50/+100」三个快捷加按钮
   - 「♾ 无限转」开关,切换立即保存
   - 状态条显示"保存中…/✓ 已保存/⚠ 错误"
   - 主播无需进 admin 即可调整次数(用 admin 鉴权,token 跨 tab 共享)

2. **没次数时 START 按钮禁用**
   - 主转盘模式下 spinCount=0 且非无限 → spinBtn / sideSpinBtn disabled,grayscale
   - 真心话/大冒险模式下不影响(只有主转盘扣次数,直接抽题不扣)
   - resultTip 显示"⚠ 转动次数已用完,请补充次数后再抽"

3. **规则调整:猜对默认 -1**
   - 之前默认猜对 +1(返还),猜错 +1~10
   - 现在默认猜对 -1(再扣),猜错 +1~10
   - admin 「猜对增量」字段从 select 改为 number input,允许负数(-100 ~ +100)
   - server.js applyGuess 不再 `Math.max(0, ...)`,允许负 delta
   - submitGuess 显示文案处理负数情况

4. **admin token 改用 localStorage**(关键!)
   - 之前是 sessionStorage(per-tab,关浏览器失效)
   - 现在是 localStorage(跨 tab + 关浏览器还在)
   - 原因:主界面也要调 admin 接口改 spinCount,如果 sessionStorage 主界面 tab 拿不到 admin tab 的 token,会被 401
   - 安全权衡:这是内网/朋友聚会工具,体验 > 严格安全;关浏览器持久登录可接受

**改动文件**(7 个):
- `server.js`(允许负 guess delta + 默认 -1)
- `public/index.html`(主界面新区块 #spinControlCard)
- `public/wheel.js`(updateSpinCountDisplay 加禁用逻辑 + saveSpinControl debounce + setMode 重新评估)
- `public/admin.html`(guessRewardInput 改 number input)
- `public/admin.js`(允许负数解析)
- `public/common.js`(token sessionStorage → localStorage)
- `public/style.css`(.spin-control-card 全套样式 + .is-disabled-empty)

**注意事项**(下次改这里):
- 主界面调 admin 接口需要 token。如果设了密码且 token 不存在,saveSpinControl 会显示"主播需先到控制台登录"。可以考虑在主界面也加个登录浮层,但目前提示语足够清晰。
- 若主播切换 admin 模式(密码新设/改),已有的 token 失效,需重新登录。
- localStorage 跨 tab,主播在控制台登录后所有 tab 共享。

### v2.5.0 — 转动次数池 + 猜左右玩法（2026-05）

**新增运营互动机制**:

主播在 admin 控制台预置剩余转动次数,玩家每次点 START(主转盘启动)消耗 1 次。次数为 0 时玩家无法继续抽,直到主播充值。`无限转`开关可绕过次数限制。

大冒险结果弹窗里多一个`猜左右`小游戏(admin 可关):
- 猜对:返还 N 次(admin 可配,默认 1 = 本次抽奖白嫖)
- 猜错:增加 [min, max] 随机次数(默认 1-10)
- **正确答案在服务端随机生成**,前端 devtools 看不到,防作弊

**改动**:
- `server.js`:
  - settings 新增 6 个字段:`spinCount` / `spinUnlimited` / `guessEnabled` / `guessRewardOnCorrect` / `guessPenaltyMin` / `guessPenaltyMax`
  - 新增 `consumeSpin()` 和 `applyGuess()` 辅助函数
  - 新增 `POST /api/spin/start`(无鉴权,原子扣减,次数 0 返 409)
  - 新增 `POST /api/guess`(无鉴权,服务端生成正确答案)
- `public/admin.html` + `admin.js`:全局设置下面新增「🎲 转动次数 / 猜左右」区块,6 个控件 + 1 个保存按钮
- `public/index.html`:topbar 加 `#spinCountPill` 胶囊显示剩余次数;result-modal 加 `#guessBox` 猜左右块
- `public/wheel.js`:
  - `spin()` 改成 async,主转盘启动前先 `await fetch('/api/spin/start')`,失败/0 次时 toast 提示并 return
  - `loadServerData()` 同步剩余次数胶囊
  - `showResultModal({...,showGuess})` 大冒险时显示猜左右块
  - 新增 `updateSpinCountDisplay()` 和 `submitGuess()`
- `public/style.css`:`.spin-count-pill` / `.spin-unlimited-badge` / `.guess-box` / `.guess-btn` / `.guess-result` 全套样式

**注意事项**(下个 Claude 改这里时):
- spinCount 从来不通过 `/api/data` POST 修改 —— 玩家白名单只允许 `autoSpinNext` / `peopleCount`
- 改 spinCount 的两条路:admin 通过 `/api/admin/save` 设置初始值;玩家通过 `/api/spin/start` 和 `/api/guess` 间接改动
- 直接进入"真心话/大冒险"大转盘抽题(不经过主转盘)**不扣次数**,只有主转盘启动才扣
- 猜左右仅大冒险结果弹窗显示,真心话不显示

### v2.4.1 — 玩家页转盘小屏自适应（2026-05）

之前手机竖屏时转盘按 `94vw` / `94vmin` 算尺寸,直接吃光竖向空间,下方随机点名要滚动才能看到。

**改动**：仅 `public/style.css`，在 760px / 560px 断点加 `vh` 高度上限：
- 手机：`width: min(82vw, 56vh, 460px)`(以前 `min(94vw, 94vmin)`)
- 平板：`width: min(86vw, 60vh, 560px)`(以前 `min(92vmin, 600px)`)
- 新增极矮屏断点(`max-height: 700px and max-width: 900px`)兜底
- 顺手压缩了 `.stage` padding、result-line 和 tip 字号

效果:转盘和点名区块在常见手机/平板尺寸都能同框看到,不用滚动。

### v2.4.0 — 玩家页随机点名挪到上面（2026-05）

用户体验调整：把「随机点名」区块从控制按钮下方挪到上方。

**改动**：仅 `public/index.html`，把 `.picker-card` 和 `.quick-controls` 两个 div 在 `.wheel-layout` 里互换 DOM 顺序。CSS 是单列 grid 按 DOM 顺序堆叠，没动 CSS、没动 wheel.js。

### v2.3.0 — GM 飘屏后端补完（2026-05）

修复历史遗留：原版 admin 控制台有「📢 GM 飘屏」UI，但 server.js 从未实现 `POST /api/broadcast` 接口，点发送按钮会 `Unexpected token '<'` 报错。

**改动**：
- `server.js`：新增 `setBroadcast()` + `POST /api/broadcast`（鉴权，空内容 400）；`defaultData` / `normalizeData` / `writePlayerSafe` 全保留 broadcast 字段
- `public/admin.js`：`apiSendBroadcast` 改用 `adminHeaders()` 带 `X-Admin-Auth`，加 401 处理

**前端逻辑无需改**：wheel.js 原本就在轮询时按 `broadcast.id` 触发飘屏（`wheel.js:77-82`）。

### v2.2.0 — 控制台密码 + 内联编辑 + 单条删除（2026-05）

commit `5dd8ca1`，本对话产出。

**功能**：
1. 🔐 **控制台密码保护**
   - 全局设置区块新增「🔐 控制台密码」输入 + 保存按钮
   - 进入 `/admin.html` 时弹全屏登录遮罩（不刷新页就能登）
   - sessionStorage 持久化登录态（关浏览器即失效，安全）
   - 留空保存 = 关闭密码保护
   - 服务端 `/api/admin/{status,login,password,save}` 全套鉴权接口
   - 玩家页通过 `/api/data` POST 仅能改白名单字段（autoSpinNext / peopleCount）
   - `adminPassword` 永远不通过 GET 响应暴露

2. ✏️ **题目内联编辑**
   - 「数据预览」中点击题目文字 → 直接编辑（contenteditable）
   - Enter 保存 / Esc 取消 / 失焦自动保存
   - 改空回滚、重复内容回滚、保存失败回滚
   - 编辑保留原有权重

3. 🗑 **单条删除**
   - 每条题目右侧新增红色 🗑 按钮
   - 点击 confirm → 立即删除并刷新计数

**改动文件**：
- `server.js`（新增鉴权接口、双写入路径、密码字段保护）
- `public/admin.html`（密码区块 + 登录遮罩）
- `public/admin.js`（登录流程 / 密码保存 / 内联编辑 / 删除按钮）
- `public/common.js`（admin token 管理 + 新 API 客户端函数）
- `public/style.css`（登录遮罩样式 + 删除按钮 + 可编辑文字 hover/focus 视觉）

### v2.1.0 — Initial commit（用户上传）

commit `a8223da`。基础功能：双层转盘、JSON 题库、批量导入、权重调整、随机点名 UI。
**没有**：密码保护、内联编辑、删除按钮、broadcast 后端实现。

---

## 🚦 用户偏好（Communication Style）

- **直接给方案，不要长篇大论**。一上来先动手，结论放最后
- **被质疑能力时坦诚承认**比辩解好（本对话教训：用户给 PAT 时我曾错误说"我没法用、我没联网"，**实际上沙箱白名单里就有 github.com，PAT 推送完全可行**。下次别犯）
- 喜欢**对比表格**和**进度清单**
- 测试时**只发结果截图**，让你解读
- 提的功能要**一次性全做完**，别漏
- 部署用 `git pull`，**不喜欢下载 zip 复制粘贴**
- **重要**：用户碰到下载链接挂掉的问题，**直接走 PAT git push 路线**，别推荐"换浏览器/手机/SSH"等方案，那都是绕路

---

## ⚠️ 下一个 Claude 行动检查清单（用户明确要求）

**用户原话**：「下次我问你 有没有做个类似的 你先看看 识别本地代码遗留」

**意思**：当用户问"我们之前是不是做过 XX"或者"有没有类似的"时，**先在沙箱里查本地代码状态**，再回答。原因：

1. 沙箱里 `/home/claude/truth-dare-wheel/` 可能存有**之前没推到 GitHub 的半成品代码**（v2.8.0 的 profiles 功能就是这样在沙箱里漂了好几轮才被发现）
2. 也可能跟 GitHub 远端不同步,需要 `diff` 才看得出
3. 上来就说"我没做过 X" 是会犯大错的（v2.8.0 那次就是这种典型）

**执行流程**：
```bash
# 1. 看本地有没有相关代码
grep -rn "<关键词>" /home/claude/truth-dare-wheel/ | head -20

# 2. 对比本地和远端
cd /tmp && rm -rf check-remote && git clone --depth 1 https://github.com/qiaodaxian233/truth-dare-wheel.git check-remote 2>&1 | tail -2
diff /tmp/check-remote/server.js /home/claude/truth-dare-wheel/server.js | head -40

# 3. 然后再回答用户
```

**别**只搜过往对话(`conversation_search`)就说"没做过" —— 那只能搜聊天记录,搜不到沙箱里的代码遗留。

---

## 🔮 还可以做的方向（用户问起再做）

1. **broadcast 功能补完** —— 见避坑 C，需要新增 `/api/broadcast` 接口（鉴权）
2. **批量删除 / 批量编辑** —— 数据预览加 checkbox 多选
3. **题目搜索 / 过滤** —— 题库变多后用得到
4. **导出加密备份** —— 题库 + 密码全套打包加密下载
5. **登录失败次数限制** —— 现在密码可被无限次撞库
6. **bcrypt 密码 hash** —— 上公网前必做
7. **添加 `.gitignore`** —— 至少排除 `data/wheel-data.json` 和 `node_modules/`
8. **GM 控制台权限分级** —— 比如"只读管理员"看得到题库但改不了

---

## 📝 此文件维护

下一次 Claude 做了重大改动，请：

1. 更新 README 的版本号（package.json `version` 字段对应）
2. 在「版本历史」加一条
3. 更新「项目结构」段反映新文件
4. **推送本文件到 GitHub**（让记忆持续）

文件路径：`项目对接记忆.md`（仓库根目录）

---

*最后更新:v2.10.0(主转盘视觉对半分 + 大转盘每轮抽 10 道 + 小屏图标紧凑模式)*
*由 Claude (claude-opus-4-7) 与 qiaodaxian233 协作生成*
